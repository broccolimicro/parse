import common

chp = parallel

parallel = sequence ('||' sequence)*

sequence = internal_parallel (';' internal_parallel)*

internal_parallel = term (',' term)*

term = (assignment | select | '(' parallel ')') region?

region = '\'' dec

select = '*'? '[' expr:0 '->' parallel (('[]' expr:0 '->' parallel)* | (':' expr:0 '->' parallel)*) ']'

